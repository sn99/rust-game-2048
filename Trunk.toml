[build]
target = "index.html"
dist = "dist"
# Relative asset URLs work for user/org pages and project pages
public_url = "./"

[watch]
ignore = ["dist"]

[serve]
addresses = ["127.0.0.1"]
port = 8080
open = false
